# ==================================
# Environment
# ==================================
NODE_ENV=development

# ==================================
# App Configuration
# ==================================
# Your app's public URL (use ngrok for local development with webhooks)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Public app URL used as HTTP-Referer for external API calls (translations)
# Optional: defaults to http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Site URL for sitemap generation
# Optional: defaults to https://usebofet.com
SITE_URL=https://usebofet.com

# ==================================
# Authentication (Magic Link)
# ==================================
# Get your API key from https://magic.link/dashboard
# This enables wallet-less login and embedded wallets (Polygon + Base Sepolia)
NEXT_PUBLIC_MAGIC_API_KEY=pk_live_YOUR_MAGIC_API_KEY

# Optional: Set to "true" to enable Magic SDK test mode (for testing environments)
# NEXT_PUBLIC_MAGIC_TEST_MODE=false

# ==================================
# Blockchain / RPC (Polygon)
# ==================================
# Default Polygon RPC endpoint
NEXT_PUBLIC_POLYGON_RPC_URL=https://polygon-rpc.com

# Public Polygon RPC (publicnode) — used for balance reads, gas estimation
# Optional: defaults to https://polygon-bor-rpc.publicnode.com
NEXT_PUBLIC_POLYGON_PUBLIC_RPC_URL=https://polygon-bor-rpc.publicnode.com

# Private Polygon RPC (BlockPI) — more reliable for broadcasting transactions
# Optional: defaults to BlockPI endpoint
NEXT_PUBLIC_POLYGON_PRIVATE_RPC_URL=https://polygon.blockpi.network/v1/rpc/YOUR_KEY

# Which RPC node to use for reads: "magic" | "public" | "private"
# Optional: defaults to "public"
NEXT_PUBLIC_RPC_NODE=public

# ==================================
# Paper Trading / PMT (Base Sepolia)
# ==================================
# Supabase is required for paper trading (claims tracking, positions, etc.)
# See Supabase section below

# Private key of the faucet/vault wallet on Base Sepolia
# This wallet distributes PMT tokens and gas (ETH) to users for paper trading
# Must hold PMT (0x8CC5e000199Ad0295491Fc4f6e8CC16e7108C270) and ETH on Base Sepolia
PMT_FAUCET_PRIVATE_KEY=0xYourFaucetPrivateKey

# Public address of the PMT vault where users send PMT when placing paper trades
NEXT_PUBLIC_PMT_VAULT_ADDRESS=0xYourVaultAddress

# ==================================
# Supabase
# ==================================
# Get from https://supabase.com/dashboard
# Used for paper trading: PMT claims tracking, paper positions, and vault payouts
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your_supabase_anon_key

# ==================================
# Polymarket Builder API
# ==================================
# Get your credentials from https://polymarket.com/settings?tab=builder
# Required for placing orders and interacting with Polymarket's CLOB
POLYMARKET_BUILDER_API_KEY=your_api_key_here
POLYMARKET_BUILDER_SECRET=your_secret_here
POLYMARKET_BUILDER_PASSPHRASE=your_passphrase_here

# ==================================
# Redis (Upstash)
# ==================================
# Get from https://upstash.com
# Used for caching, rate limiting, and session management
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token

# Optional: Separate Redis instance for onboarding form submissions
UPSTASH_REDIS_REST_URL_FORMS=https://your-forms-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN_FORMS=your_forms_redis_token

# ==================================
# Gas Relayer (SendGas)
# ==================================
# Private key of the wallet that pays gas for claimGasFor() calls on Polygon
# This wallet needs POL to pay for transactions on behalf of users
GAS_RELAYER_PRIVATE_KEY=0xYourRelayerPrivateKey

# ==================================
# Fee & POL Configuration
# ==================================
# Wallet address to receive trading fees from your platform
NEXT_PUBLIC_FEE_RECIPIENT_ADDRESS=0xYourWalletAddress

# Minimum POL (Polygon native token) balance to maintain in Safe wallet for gas
# Optional: defaults to 0.5
NEXT_PUBLIC_MIN_POL_BALANCE_SAFE=0.5

# Percentage of POL to keep in Safe wallet (rest distributed to EOA)
# Optional: defaults to 50 (means 50% stays in Safe, 50% goes to EOA)
NEXT_PUBLIC_POL_SAFE_PERCENTAGE=50

# ==================================
# Analytics (Datafast)
# ==================================
# Optional: Analytics platform for tracking user behavior
NEXT_PUBLIC_DATAFAST_WEBSITE_ID=dfid_YOUR_WEBSITE_ID
DATAFAST_API_KEY=df_YOUR_API_KEY

# ==================================
# AI / OpenRouter
# ==================================
# Optional: For AI-powered translation features
# Get from https://openrouter.ai
OPENROUTER_API_KEY=sk-or-v1-YOUR_API_KEY

# ==================================
# Sentry (Error Tracking)
# ==================================
# Optional: For error tracking and monitoring
# Can also be set in .env.sentry-build-plugin
# SENTRY_AUTH_TOKEN=your_sentry_auth_token

# ==================================
# UI Feature Flags
# ==================================
# Optional: Show/hide the rebalance button in the navbar
# Set to "true" to show (also always shown in development)
# NEXT_PUBLIC_SHOW_REBALANCE_BUTTON=false
